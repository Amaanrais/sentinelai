# Phase 1 dependencies (data assembly)
datasets>=2.18.0
huggingface_hub>=0.20
pandas>=2.0
matplotlib>=3.7

# Phase 2 dependencies (training + evaluation)
scikit-learn>=1.4
joblib>=1.3
numpy>=1.26

# Transformer training. Pin to tested versions for reproducibility.
# These are the versions the Colab notebook uses.
torch>=2.2
transformers>=4.40
accelerate>=0.30

# Optional: for nicer notebook conversion
jupytext>=1.16

# Web serving
fastapi>=0.111
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Multi-model probing (scripts/probe_models.py)
openai>=1.0
anthropic>=0.40
google-generativeai>=0.8
python-dotenv>=1.0
tqdm>=4.0
